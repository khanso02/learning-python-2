import math

def pidecimals():
	pi = float(22/7)
	n= raw_input("enter how many digits of pi you want:")
	precision = int(n)
	if n is done:
		quit()
	elif precision > 50:
		print "too many decimal places"
	else:
		print '%.*f' % (precision, math.pi)
	pidecimals()
pidecimals()
